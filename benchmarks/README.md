# Benchmarks

This folder contains balanced single-hop and multi-hop benchmark subsets plus scripts for resampling or inspecting distribution shifts.

Generated backups and temporary samples were removed. If you rerun sampling, write new outputs under a fresh experiment directory and do not commit transient backups.
